# Minimal, cross-platform deps to run Commons Harvest Open and this branch.
# Tried to add the additional requirements, but not sure if it will work for a fresh user, 
# please just let me knwo if you get any errors and I will update this

# Core
numpy>=1.24
matplotlib>=3.7
Pillow>=10.0
ml-collections>=0.1.1
immutabledict>=4.1.0
reactivex>=1.7.0
scikit-learn>=1.3
absl-py>=1.4.0

# Melting Pot stack
dm-env>=1.6
dmlab2d==1.0.*           # <-- was >=1.5; 1.0.* is what exists for Py3.11/mac
dm-meltingpot==2.3.*     # keep compatible with dmlab2d 1.0.*
chex>=0.1.85

# TensorFlow (only if you actually need it)
tensorflow>=2.14 ; platform_system != "Darwin"
tensorflow-macos>=2.14 ; platform_system == "Darwin"
tensorflow-metal>=1.0 ; platform_system == "Darwin"

# Optional (GPU-only, Linux x86_64); NEVER installs on macOS
# bitsandbytes>=0.40.0 ; platform_system == "Linux" and platform_machine == "x86_64"
